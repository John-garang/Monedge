services:
  - type: web
    name: monedge
    env: static
    buildCommand: echo "No build needed"
    staticPublishPath: ./
    headers:
      - path: /*
        name: X-Frame-Options
        value: SAMEORIGIN
    routes:
      - type: rewrite
        source: /(.*?)
        destination: /$1.html
